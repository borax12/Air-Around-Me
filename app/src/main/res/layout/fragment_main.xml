<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivityFragment">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/stock_background"
        android:scaleType="centerCrop"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/homescreen_background"
        android:alpha="0.2">
    </LinearLayout>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center|bottom"
        android:layout_marginBottom="@dimen/air_button_bottom_margin">
        <Button
            android:id="@+id/air_around_button"
            android:layout_width="wrap_content"
            android:layout_height="50dp"
            android:padding="@dimen/button_text_padding"
            android:text="@string/air_around_me"
            android:textSize="@dimen/air_around_button_size"
            android:textColor="@color/air_around_button_color"
            android:clickable="true"
            android:background="@drawable/transparent_stroke_button"
            android:elevation="0dp" />
    </LinearLayout>

</RelativeLayout>
